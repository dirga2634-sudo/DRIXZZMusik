# Add project specific ProGuard rules here.
# CameraX and Compose bundle their own consumer-rules.pro, so no manual
# keep rules are required for them. This file is kept minimal on purpose;
# minification is currently disabled (see app/build.gradle.kts) so this
# only takes effect once isMinifyEnabled is turned back on.
